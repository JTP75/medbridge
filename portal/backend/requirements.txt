fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
httpx>=0.27,<1

# Path dependency on the shared schema package, resolved relative to the
# current working directory — always `pip install -r requirements.txt` from
# *inside* this directory (portal/backend/), not from the repo root.
-e ../../schema
